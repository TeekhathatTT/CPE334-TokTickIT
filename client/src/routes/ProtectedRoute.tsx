// Lab 3 scaffold stub — no component markup in this pass.
// TODO(auth branch): route guard (redirect unauthenticated users to login,
// enforce mustChangePassword flow).
export function ProtectedRoute() {
  return null;
}
