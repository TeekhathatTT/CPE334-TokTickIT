// Lab 3 scaffold stub — no business logic in this pass.
// TODO(auth branch): centralized safe-error responses using the documented
// { error: { code, message, fields? } } envelope. No stack traces to clients.
export {};
