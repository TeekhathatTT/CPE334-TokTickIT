// Lab 3 scaffold stub — no business logic in this pass.
// TODO(auth branch): verify session/token (opaque server-side session cookie
// `toktickit_session`), attach req.user. Safe 401 errors, no enumeration.
export {};
